<!-- Solid app floor. Must sit BEHIND the banner (-z-20), so it's further back. -->
<div class="fixed inset-0 -z-30 bg-background"></div>
<div class="theme-studio-backdrop pointer-events-none fixed inset-0 -z-20" aria-hidden="true"></div>
